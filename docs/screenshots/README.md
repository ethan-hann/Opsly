# Screenshots

Drop the README's screenshots here. The root `README.md` references these exact
filenames, so match them (or update the links there):

| File | Shows |
| --- | --- |
| `dashboard-light.png` | The main dashboard in light mode |
| `dashboard-dark.png` | The main dashboard in dark mode |

Tips:

- Capture at a consistent width (e.g. 1440px) so the two line up in the README table.
- PNG for UI screenshots; keep each well under ~1 MB.
- Use realistic-but-fake sample data — no real customer names, emails, or org data.
